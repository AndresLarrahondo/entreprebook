<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h1>Book</h1>
      <div class="text-right">
        <%= link_to "Publica Book", new_book_path, class: "btn btn-default"%>
      </div>
      <table class="table">
        <tr>
          <th>Id</th>
          <th>Nombre</th>
          <th>Url</th>
          <th>Descripción</th>
        </tr>
        <% @books.each do |book| %>
         <tr>
           <td><%= book.id %></td>
           <td><%= book.name %></td>
           <td><%= book.url %></td>
           <td><%= book.description %></td>
         </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
